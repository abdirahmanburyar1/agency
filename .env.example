# ===========================================
# Daybah Travel Agency - Environment Variables
# Copy to .env.local and set your values
# ===========================================

# --- Database (Prisma) ---
# Local: use same value for both (e.g. Docker Postgres)
# DATABASE_URL="postgresql://daybah:daybah_dev@localhost:5432/daybah_travel"
# DIRECT_URL="postgresql://daybah:daybah_dev@localhost:5432/daybah_travel"

# Neon (prod / Vercel): paste from Neon dashboard. Prisma needs:
#   DATABASE_URL = pooled (recommended) — use POSTGRES_PRISMA_URL or the -pooler URL
#   DIRECT_URL   = unpooled — use DATABASE_URL_UNPOOLED or POSTGRES_URL_NON_POOLING
DATABASE_URL="postgresql://neondb_owner:<YOUR_NEON_PASSWORD>@ep-summer-frost-ailg6m4g-pooler.c-4.us-east-1.aws.neon.tech/neondb?connect_timeout=15&sslmode=require"
DIRECT_URL="postgresql://neondb_owner:<YOUR_NEON_PASSWORD>@ep-summer-frost-ailg6m4g.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Neon also provides (optional): PGHOST, PGHOST_UNPOOLED, PGUSER, PGDATABASE, PGPASSWORD,
# POSTGRES_* (POSTGRES_URL, POSTGRES_URL_NON_POOLING, POSTGRES_PRISMA_URL, etc.)

# --- Auth (NextAuth) ---
AUTH_SECRET=""
# Local:
NEXTAUTH_URL="http://localhost:3000"
# Vercel (prod): set in project env
# NEXTAUTH_URL="https://agency-eight-taupe.vercel.app"

# Optional: for seed only. If set and no users exist, seed creates an admin.
# ADMIN_INITIAL_EMAIL="admin@example.com"
# ADMIN_INITIAL_PASSWORD="your-secure-password"
# Note: The initial migration also creates an admin: admin@daybah.com / ChangeMe1! — change after first login.

# --- ImageKit ---
IMAGEKIT_PUBLIC_KEY=""
IMAGEKIT_PRIVATE_KEY=""
IMAGEKIT_URL_ENDPOINT=""
# Local:
IMAGEKIT_AUTH_ENDPOINT="http://localhost:3000/api/imagekit/auth"
# Vercel (prod): set in project env
# IMAGEKIT_AUTH_ENDPOINT="https://agency-eight-taupe.vercel.app/api/imagekit/auth"

# --- Pusher (optional) ---
NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER="mt1"
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
