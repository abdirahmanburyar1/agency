# ===========================================
# Daybah Travel Agency - Environment Variables
# Copy to .env.local and fill in your values
# ===========================================

# --- Database ---
# Dev: Docker Postgres (docker-compose up -d)
DATABASE_URL="postgresql://daybah:daybah_dev@localhost:5432/daybah_travel"

# Prod: Set in Vercel from Vercel Postgres / Neon integration

# --- Auth ---
# Required. Generate: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# Required for NextAuth client-side session fetch. Use your app URL.
# Dev: http://localhost:3000
# Prod: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# --- ImageKit (https://imagekit.io/dashboard) ---
IMAGEKIT_PUBLIC_KEY="public_3ab1ZdlVQA3wNI5zxbbZOKeyFA8="
IMAGEKIT_PRIVATE_KEY="private_OXVNr4+bM9EudsRuLVvgPiikgxA="
IMAGEKIT_URL_ENDPOINT="https://ik.imagekit.io/jj4328nr6"
IMAGEKIT_AUTH_ENDPOINT="http://localhost:3000/api/imagekit/auth"

# Prod: IMAGEKIT_AUTH_ENDPOINT="https://yourdomain.com/api/imagekit/auth"

# --- Pusher (https://pusher.com) - Real-time updates ---
NEXT_PUBLIC_PUSHER_KEY="your-pusher-key"
NEXT_PUBLIC_PUSHER_CLUSTER="mt1"
PUSHER_APP_ID="your-pusher-app-id"
PUSHER_KEY="your-pusher-key"
PUSHER_SECRET="your-pusher-secret"
